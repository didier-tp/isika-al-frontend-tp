Pour visualiser les énoncés de TD/TP de ce répertoire il faut :
- effectuer une copie locale de ce répertoire (via git clone ou extraction de .zip ou autre)
- afficher le fichiers HTML dans un navigateur (via double click ou open with ...)